# Transfer Abkhazia
Для запуска бэкенда:
```shell
cd backend
poetry install
poetry shell
python manage.py runserver
```

Для запуска фронтенда:
```shell
cd frontend
pnpm install
pnpm run
```
Вместо `pnpm` можно использовать `npm`.
